#!/bin/bash
set -o errexit
VERSION='1.3.2
URL='http://rubyforge.org/frs/download.php/69365/rubygems-1.3.2.tgz'

# ruby setup.rb --prefix=/usr --destdir=/stow/rubygems-1.3.6/ --no-rdoc --no-ri # Didn't work TDW 2010 April

# PREFIX="/usr/lib/ruby"
DESTDIR="/stow/rubygems-${VERSION}"
sudo mkdir -v /stow/rubygems-${VERSION}
sudo chown -v troy /stow/rubygems-${VERSION}
sleep 5
# ruby setup.rb --prefix=${PREFIX} --destdir=${DESTDIR} --no-rdoc --no-ri
ruby setup.rb --destdir=${DESTDIR} --no-rdoc --no-ri
sudo stow /stow/rubygems-${VERSION}
