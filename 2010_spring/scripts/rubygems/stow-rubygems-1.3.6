#!/bin/bash
set -o errexit

URL='http://rubyforge.org/frs/download.php/69365/rubygems-1.3.6.tgz'

# ruby setup.rb --prefix=/usr --destdir=/stow/rubygems-1.3.6/ --no-rdoc --no-ri # Didn't work TDW 2010 April

PREFIX="/usr/lib/ruby"
DESTDIR="/stow/rubygems-1.3.6"
sudo mkdir -v /stow/rubygems-1.3.6
sudo chown -v troy /stow/rubygems-1.3.6
sleep 5
ruby setup.rb --prefix=${PREFIX} --destdir=${DESTDIR} --no-rdoc --no-ri
