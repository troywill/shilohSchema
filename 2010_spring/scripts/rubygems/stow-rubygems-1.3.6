#!/bin/bash
set -o errexit
VERSION="1.3.6"
RUBY_VERSION="1.9.1p376"
# PREFIX="/usr" # tried on April 16, 2010 but gems lib wasn't loaded
PREFIX="/usr/lib/ruby/1.9.1"
DESTDIR="/stow/rubygems-1.3.6"

sudo mkdir -v --parent ${DESTDIR}
sudo chown -v troy ${DESTDIR}

ruby setup.rb \
    --destdir=${DESTDIR} \
    --no-rdoc \
    --no-ri

# --prefix=${PREFIX} \